# 지식 그래프 산출물(선택 연동, docs/setup.md 참조) 중 크고 자주 다시 쓰이는
# 파일만 막는다. 도구가 워크스페이스에 파일을 쓸 때마다 Claude Code 프롬프트
# 캐시가 무효화되어 다음 턴에 전체 재업로드가 발생한다.
graphify-out/graph.json
graphify-out/graph.html
graphify-out/cache/

# graphify-out/GRAPH_REPORT.md는 일부러 뺐다 — 그래프를 붙인 저장소는
# wiki/routing.json의 research 의도가 이 파일을 reference 후보로 내보낼 수
# 있다. 여기서 막으면 라우팅이 열라고 건넨 문서를 열 수 없다(route.py는
# 존재만 확인하고 읽을 수 있는지는 보지 않아 종료 코드 0으로 통과한다).
