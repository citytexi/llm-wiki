# 작업 로그

append-only. 새 항목은 파일 끝에 추가한다.
`grep "^## \[" wiki/log.md | tail -10` 으로 최근 10개 확인.

항목 형식은 `## [YYYY-MM-DD] <의도> | <한 줄 요약>` 이다. 의도는
`wiki/CLAUDE.md`가 정의한 8종 중 하나를 쓴다. 필요하면 아래에 세부 항목을
붙인다.

아직 기록이 없다. 첫 작업을 마치면 여기에 첫 항목이 붙는다.
